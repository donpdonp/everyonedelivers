<!-- openid start -->
 <script>
</script>

<div class="header">
  <div id="sub">
  <%= form_tag({:controller => :session, :action => :login}, {:id => 'loginform'}) do %>
    <span id="geomsgaddress"></span>
    <span id="geomsgacctime"></span>
    <% if user_signed_in? %>
      <%= link_to h(current_user.username), current_user, :class => "username" %> -
      <%= link_to t('header.logout'), :controller => :session, :action => :logout %>
    <% else %>
      Email: <%= text_field_tag :email, nil, :placeholder => t('header.email_login_placeholder'), :size => 20 %>
      <%= submit_tag t('header.login') %>
    <% end %>
  <% end %>
  </div>
    <%= link_to image_tag('logo.png'), :root %>
</div> 

<script>
/*
if (navigator.geolocation) {  
   $('geomsgaddress').innerHTML = "looking up geolocation";
} */
</script>
