edit user
<% form_for :user, @user, :url => user_path(@user.username), :html => { :method => :put } do |f| %>
  <fieldset>
    username: <%= f.text_field :username %>
  </fieldset>
  <br/>
  <%= f.submit %>
<% end %>
